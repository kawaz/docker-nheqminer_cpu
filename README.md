# Usage

```
# Donate
docker run -d kawaz/nheqminer_cpu

# Easy way
docker run -d -e WALLET=YourBtcAddreshOfNicehash kawaz/nheqminer_cpu

# Free way, you can use custom parameters
docker run -d kawaz/nheqminer_cpu -h
```
